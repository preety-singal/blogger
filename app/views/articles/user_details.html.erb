<head>
<style type="text/css">
	.btn{
    line-height: 0.4571;;
  }
  body{
  	height: 600px;
  }
</style>
</head>

<div class="col-md-4"><h4>User Email</h4></div>
    <div class="col-md-4"><h4>Role</h4></div>
    <div class="col-md-4"><h4>Change Role</h4></div>
    
  <% @user.each do |user| %>
  <% if user.is_active==true %>
  <div class="col-md-4">
     <%=  user.email%></div>
     <div class="col-md-4"><%= user.roles%></div>
      <%if user_signed_in?%>

    <% if user.roles==true%>
    <div class="col-md-4"> NA </div>
    <% else %>
     <div class="col-md-4"><%= link_to "promote",  change_role_path(:user_id=>user.id)%></div>

    <% end %>
    <% end %>
    <%end %>
    <% end %>
     
   <%= link_to " Back to Articles List", articles_path, :class => 'new_article' %>